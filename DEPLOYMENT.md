# Инструкция по развертыванию на Render.com

## Шаг 1: Подготовка GitHub

### 1.1 Инициализация Git репозитория

```bash
# Проверьте, не инициализирован ли уже git
git status

# Если git не инициализирован, выполните:
git init
git add .
git commit -m "Initial commit: WB seller application"
```

### 1.2 Создание репозитория на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository" (зеленая кнопка)
3. Введите название репозитория (например, `wb-seller-app`)
4. Оставьте репозиторий **ПРИВАТНЫМ** (важно для безопасности!)
5. НЕ добавляйте README, .gitignore или лицензию (у вас они уже есть)
6. Нажмите "Create repository"

### 1.3 Подключение к GitHub

```bash
# Замените YOUR_USERNAME и YOUR_REPO на ваши данные
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

## Шаг 2: Развертывание на Render.com

### 2.1 Регистрация

1. Перейдите на https://render.com
2. Нажмите "Get Started"
3. Зарегистрируйтесь через GitHub
4. Разрешите Render доступ к вашим репозиториям

### 2.2 Создание Web Service

1. На панели Render нажмите "New +"
2. Выберите "Web Service"
3. Найдите ваш репозиторий `wb-seller-app` и нажмите "Connect"

### 2.3 Настройка сервиса

**Основные настройки:**
- **Name**: `wb-seller-app` (или любое другое)
- **Region**: Выберите ближайший регион (например, Frankfurt)
- **Branch**: `main`
- **Root Directory**: оставьте пустым
- **Runtime**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `gunicorn app:app`

**Plan:**
- Выберите **Free** (бесплатный план)

### 2.4 Добавление переменных окружения (Environment Variables)

Нажмите "Advanced" и добавьте следующие переменные:

```
SECRET_KEY=YOUR_SECRET_KEY_HERE
ENCRYPTION_KEY=YOUR_ENCRYPTION_KEY_HERE
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET
DATABASE_URL=sqlite:///app.db
```

**Как получить значения:**

1. **SECRET_KEY** - выполните локально:
   ```bash
   python -c "import secrets; print(secrets.token_hex(32))"
   ```

2. **ENCRYPTION_KEY** - выполните локально:
   ```bash
   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
   ```

3. **GOOGLE_CLIENT_ID** и **GOOGLE_CLIENT_SECRET** - из Google Cloud Console

4. Нажмите "Create Web Service"

### 2.5 Обновление Google OAuth Redirect URI

После создания сервиса, Render выдаст вам URL (например, `https://wb-seller-app.onrender.com`)

1. Перейдите в Google Cloud Console
2. Откройте ваше OAuth приложение
3. В "Authorized redirect URIs" добавьте:
   ```
   https://your-app-name.onrender.com/auth/callback
   ```
4. Сохраните изменения

## Шаг 3: Инициализация базы данных

После первого успешного деплоя:

1. В панели Render перейдите в "Shell" вашего сервиса
2. Выполните:
   ```bash
   python migrate_add_defects_to_finished_goods.py
   ```

## Шаг 4: Проверка работы

1. Откройте URL вашего приложения
2. Попробуйте войти через Google
3. Проверьте основные функции

## Важные замечания

### Ограничения бесплатного плана Render:

- ⏰ Сервис "засыпает" после 15 минут неактивности
- 🐌 Первый запрос после "сна" занимает ~30-60 секунд
- 💾 750 часов работы в месяц (достаточно для тестирования)
- 📦 Дисковое пространство ограничено

### Альтернативы Render.com:

**Railway.app:**
- Более щедрый бесплатный tier
- $5 кредитов в месяц
- Проще в настройке

**PythonAnywhere:**
- Специализируется на Python приложениях
- Постоянная работа (не засыпает)
- Меньше ресурсов на бесплатном плане

**Fly.io:**
- Хорошая производительность
- Более сложная настройка
- Требует кредитной карты

## Обновление приложения

После изменения кода:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Render автоматически задеплоит новую версию.

## Переход на PostgreSQL (рекомендуется для продакшена)

Для стабильной работы лучше использовать PostgreSQL вместо SQLite:

1. В Render создайте "PostgreSQL" базу данных (есть в бесплатном плане)
2. Скопируйте "Internal Database URL"
3. Обновите переменную окружения `DATABASE_URL`
4. Добавьте в requirements.txt: `psycopg2-binary`
5. Передеплойте приложение

## Мониторинг и логи

- **Логи**: В панели Render → "Logs"
- **Метрики**: В панели Render → "Metrics"
- **Shell**: Для выполнения команд на сервере

## Безопасность

✅ **Сделано:**
- Приватный репозиторий
- Переменные окружения на Render
- `.env` файл в `.gitignore`
- Шифрование API ключей

⚠️ **Дополнительно:**
- Регулярно обновляйте зависимости
- Мониторьте логи на ошибки
- Сделайте бэкап базы данных

## Поддержка

Если возникли проблемы:
1. Проверьте логи в Render
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что OAuth redirect URI правильный
4. Обратитесь к документации Render: https://render.com/docs
