# 🚀 Быстрый старт: Деплой за 15 минут

Самый простой способ развернуть приложение для общего доступа.

## Вариант 1: Render.com (Рекомендуется)

### Шаг 1: Подготовка (5 минут)

1. **Создайте аккаунт на GitHub** (если нет): https://github.com/signup

2. **Загрузите код на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ВАШ_USERNAME/wb-claude.git
   git branch -M main
   git push -u origin main
   ```

3. **Сгенерируйте ключи безопасности:**
   ```bash
   python generate_keys.py
   ```
   **Скопируйте и сохраните** оба ключа - они понадобятся дальше!

### Шаг 2: Настройка Google OAuth (3 минуты)

1. Откройте https://console.cloud.google.com/
2. Выберите ваш проект
3. **APIs & Services** → **Credentials** → выберите ваш OAuth 2.0 Client
4. В **Authorized redirect URIs** добавьте:
   ```
   https://ВАШ-АПП.onrender.com/auth/callback
   ```
   (замените `ВАШ-АПП` на имя, которое выберете на следующем шаге)
5. Нажмите **SAVE**

### Шаг 3: Деплой на Render.com (7 минут)

1. **Зарегистрируйтесь на Render:**
   - Перейдите на https://render.com
   - Нажмите **Get Started**
   - Выберите **Sign up with GitHub**

2. **Создайте PostgreSQL базу данных:**
   - В Dashboard нажмите **New +**
   - Выберите **PostgreSQL**
   - Name: `wb-claude-db`
   - Database: `wb_claude`
   - User: `wb_claude_user`
   - Выберите **Free** plan
   - Нажмите **Create Database**
   - ⏱️ Подождите 2-3 минуты, пока база создается
   - **Скопируйте Internal Database URL** (он нужен на следующем шаге)

3. **Создайте Web Service:**
   - Нажмите **New +** → **Web Service**
   - Выберите **Connect a repository** → подключите ваш GitHub репозиторий
   - Настройки:
     - **Name:** `wb-claude` (или любое другое)
     - **Environment:** `Python 3`
     - **Build Command:** `pip install -r requirements.txt`
     - **Start Command:** `gunicorn app:app`
     - **Instance Type:** `Free`

4. **Добавьте переменные окружения:**

   Нажмите **Advanced** → **Add Environment Variable** и добавьте:

   | Variable Name | Value |
   |--------------|-------|
   | `SECRET_KEY` | *ключ из generate_keys.py* |
   | `ENCRYPTION_KEY` | *ключ из generate_keys.py* |
   | `DATABASE_URL` | *Internal Database URL из шага 2* |
   | `GOOGLE_CLIENT_ID` | *из Google Console* |
   | `GOOGLE_CLIENT_SECRET` | *из Google Console* |
   | `FLASK_ENV` | `production` |

5. **Нажмите Create Web Service**

6. ⏱️ **Подождите 5-10 минут** - Render развернет приложение

### Шаг 4: Инициализация базы данных (2 минуты)

После успешного деплоя:

1. В Render Dashboard откройте ваш Web Service
2. Перейдите на вкладку **Shell**
3. Выполните команды:
   ```python
   python
   >>> from app import app, db
   >>> with app.app_context():
   ...     db.create_all()
   >>> exit()
   ```

### Шаг 5: Готово! 🎉

Ваше приложение доступно по адресу:
```
https://ВАШ-АПП.onrender.com
```

---

## Вариант 2: Railway.app (Еще проще!)

### Шаг 1: Подготовка (2 минуты)

1. Загрузите код на GitHub (см. выше)
2. Сгенерируйте ключи: `python generate_keys.py`

### Шаг 2: Деплой (5 минут)

1. Перейдите на https://railway.app
2. Нажмите **Start a New Project**
3. Выберите **Deploy from GitHub repo**
4. Выберите репозиторий `wb-claude`
5. **Railway автоматически определит Python** и начнет деплой!

### Шаг 3: Добавьте PostgreSQL (1 минута)

1. В вашем проекте нажмите **New**
2. Выберите **Database** → **Add PostgreSQL**
3. Railway автоматически создаст переменную `DATABASE_URL`

### Шаг 4: Добавьте переменные окружения (2 минуты)

1. Откройте ваш сервис
2. Перейдите в **Variables**
3. Добавьте:
   ```
   SECRET_KEY=<из generate_keys.py>
   ENCRYPTION_KEY=<из generate_keys.py>
   GOOGLE_CLIENT_ID=<из Google Console>
   GOOGLE_CLIENT_SECRET=<из Google Console>
   FLASK_ENV=production
   ```

### Шаг 5: Настройка Google OAuth и инициализация БД

1. Скопируйте URL вашего приложения (показан в Dashboard)
2. Добавьте в Google Console redirect URI:
   ```
   https://ваш-домен.railway.app/auth/callback
   ```
3. Инициализируйте базу через Shell (как в Render)

### Готово! 🚀

---

## 🆘 Частые проблемы

### "Application failed to respond"
- Проверьте логи (Logs tab в Render/Railway)
- Убедитесь, что все переменные окружения добавлены
- Проверьте, что `DATABASE_URL` правильный

### "Invalid redirect_uri"
- Проверьте, что в Google Console добавлен правильный redirect URI
- URL должен точно совпадать (включая https://)
- Может потребоваться 5-10 минут для применения изменений

### "Database connection failed"
- Убедитесь, что PostgreSQL база создана
- Проверьте, что `DATABASE_URL` скопирован правильно
- Для Render используйте **Internal Database URL**, не External

### Приложение "засыпает" на Render Free
- Это нормально! На бесплатном плане приложение засыпает через 15 минут неактивности
- Первый запрос после сна займет 30-60 секунд
- Решение: перейти на платный план ($7/месяц) или использовать Railway ($5 бесплатно/месяц)

---

## 📱 Следующие шаги

После успешного деплоя:

1. **Протестируйте все функции:**
   - Авторизация через Google
   - Добавление API ключа Wildberries
   - Создание сессии
   - Загрузка товаров

2. **Настройте доменное имя** (опционально):
   - На Render: Settings → Custom Domain
   - На Railway: Settings → Domains

3. **Настройте мониторинг:**
   - Добавьте UptimeRobot для проверки доступности
   - Настройте email уведомления об ошибках

4. **Создайте бэкапы:**
   - Render: автоматические бэкапы на платном плане
   - Railway: используйте pgdump для ручных бэкапов

---

## 💰 Стоимость

### Render.com Free:
- ✅ Бесплатно навсегда
- ⚠️ Засыпает после 15 минут неактивности
- ⚠️ 750 часов работы в месяц
- Подходит для: тестирования, личного использования

### Railway.app:
- ✅ $5 бесплатно каждый месяц
- ✅ Не засыпает
- ⚠️ После $5 - платно (~$10-20/месяц)
- Подходит для: малого бизнеса

### VPS (DigitalOcean/Hetzner):
- 💰 От $6/месяц
- ✅ Полный контроль
- ✅ Не засыпает
- Подходит для: серьезного использования

---

Удачи! 🎉
